load("@rules_cc//cc:defs.bzl", "cc_import")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "wolfssl",
    srcs = glob(["**/build/**/*.lo"]) + glob(["**/build/**/*.so*"]),
    hdrs = glob(["**/wolfssl/**/*.h"]),
)
